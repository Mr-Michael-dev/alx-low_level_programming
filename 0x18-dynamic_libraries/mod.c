#include "main.h"

/**
 * mod - computes the remainder of an integers
 * @a: value to check remainder
 * @b: value of denominator
 *
 * Return: The remainder
 */

int mod(int a, int b)
{
	return (a % b);
}
