#include "main.h"

/**
 * sub - computes the difference between two integer
 * @a: value to subtract from
 * @b: value to subtract
 *
 * Return: subtraction
 */

int sub(int a, int b)
{
	return (a - b);
}
