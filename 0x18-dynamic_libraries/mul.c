#include "main.h"

/**
 * mul - computes the multiplication of integers
 * @a: value to be multiplied
 * @b: value to be multiplied
 *
 * Return: The multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
