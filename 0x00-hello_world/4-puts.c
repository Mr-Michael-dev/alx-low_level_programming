#include <stdio.h>

/**
 * main - entry point
 *
 * return: 0 (exits on success)
 */

int main(void)
{
	const char *program = "Programming is like building a multilingual puzzle";

	puts(program);

	return (0);
}
